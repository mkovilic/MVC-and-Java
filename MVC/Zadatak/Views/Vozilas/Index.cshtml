@model IEnumerable<Zadatak.Models.Database.Vozila>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tip)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Marka)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GodinaProizvodnje)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StanjeKilometra)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marka)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StanjeKilometra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GodinaProizvodnje)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IDVozilo }) |
                @Html.ActionLink("Details", "Details", new { id = item.IDVozilo }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IDVozilo })
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Servis", "Index", "Servis1", null, null) |
@Html.Partial("~/Views/Shared/_Navigation.cshtml")
